@model FYP.Models.Employee
@{
    ViewData["Title"] = "Guest Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    ViewData["ShowGuestName"] = true;
    ViewData["FormController"] = "Guest";
}

<div class="container mt-5">
    <h2>@L["Welcome Guest!"]</h2>
    <p class="lead">@L["This is your guest dashboard page."]</p>

    <div class="mt-4">
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">@L["Back to Home"]</a>
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">@L["Log out"]</a>

   </div>
</div>
