@model IEnumerable<FYP.Models.Table>
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<FYP.Localization.SharedResource> L
@{
    ViewData["Title"] = L["Tables"];
    Layout = "~/Views/Shared/_Layout.cshtml";

    var stats = new List<FYP.ViewModels.AdminStatViewModel>();
    if (Model != null && Model.Any())
    {
        stats.Add(new FYP.ViewModels.AdminStatViewModel { Icon = "table_restaurant", Number = Model.Count().ToString(), Label = L["Total Tables"], ColorClass = "stat-cube-gold" });
        stats.Add(new FYP.ViewModels.AdminStatViewModel { Icon = "check_circle", Number = Model.Count(t => t.IsAvailable).ToString(), Label = L["Available"], ColorClass = "stat-cube-green" });
        stats.Add(new FYP.ViewModels.AdminStatViewModel { Icon = "link", Number = Model.Count(t => t.IsJoinable).ToString(), Label = L["Joinable"], ColorClass = "stat-cube-blue" });
        stats.Add(new FYP.ViewModels.AdminStatViewModel { Icon = "groups", Number = Model.Sum(t => t.Capacity).ToString(), Label = L["Total Capacity"], ColorClass = "stat-cube-red" });
    }

    ViewBag.SearchPlaceholder = "Search tables...";
    ViewBag.SearchInputId = "TablesSearchInput";

    // Choose back target based on role so admins don't hit manager-only routes
    var backController = User.IsInRole("admin") ? "Admin" : "Manager";
}

<div class="container-custom" style="padding: var(--spacing-3xl) var(--spacing-2xl); max-width: 1400px; margin: 0 auto;">
    <partial name="~/Views/Shared/_BackButton.cshtml" model="@Url.Action("Index", backController)" />

    @if (TempData["Success"] != null)
    {
        { ViewData["Type"] = "success"; ViewData["Message"] = TempData["Success"]; }
        <partial name="~/Views/Shared/_AlertMessage.cshtml" />
    }

    @if (TempData["Info"] != null)
    {
        { ViewData["Type"] = "info"; ViewData["Message"] = TempData["Info"]; }
        <partial name="~/Views/Shared/_AlertMessage.cshtml" />
    }

    @if (TempData["Error"] != null)
    {
        { ViewData["Type"] = "error"; ViewData["Message"] = TempData["Error"]; }
        <partial name="~/Views/Shared/_AlertMessage.cshtml" />
    }

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center" style="margin-bottom: var(--spacing-2xl); padding: 0 var(--spacing-md);">
        <div>
            <h1 class="gold-heading" style="font-size: 2.5rem; margin-bottom: var(--spacing-sm);">
                @ViewData["Title"]
            </h1>
            <p style="color: var(--text-secondary);">@L["Manage restaurant table configuration"]</p>
        </div>
        <partial name="~/Views/Shared/_Search.cshtml" />
    </div>

    @if (Model != null && Model.Any())
    {
        <!-- Stats Row -->
        <div class="stats-row-container" style="margin-bottom: var(--spacing-3xl); padding: 0 var(--spacing-md);">
            @foreach (var s in stats)
            {
                @Html.Partial("~/Views/Shared/_StatCube.cshtml", s)
            }
        </div>

        <div style="padding: 0 var(--spacing-md); margin-bottom: var(--spacing-xl);">
            <a asp-controller="Tables" asp-action="TableJoins" class="btn btn-gold" role="button" style="margin-bottom: var(--spacing-sm);">
                <span class="material-symbols-outlined" style="vertical-align: middle; margin-right: 0.4rem;">link</span>
                Join Tables
            </a>
        </div>

        <!-- Table Cards Grid -->
        <div class="row g-4" style="padding: 0 var(--spacing-md); margin-top: var(--spacing-xl);">
            @foreach (var table in Model.OrderBy(t => t.TableNumber))
            {
                <div class="col-md-6 col-lg-4 col-xl-3">
                    <div class="table-card">
                        <div class="table-card-header">
                            <div class="table-number-badge">
                                <span class="table-number">@table.TableNumber</span>
                            </div>
                            <div class="table-badges">
                                @if (table.IsAvailable)
                                {
                                    <span class="table-status-badge status-available">@L["Available"]</span>
                                }
                                else
                                {
                                    <span class="table-status-badge status-unavailable">@L["Unavailable"]</span>
                                }
                            </div>
                        </div>

                        <div class="table-card-body">
                            <div class="table-detail-item">
                                <div class="table-detail-icon">
                                    <span class="material-symbols-outlined">groups</span>
                                </div>
                                <div>
                                    <div class="table-detail-label">@L["Capacity"]</div>
                                    <div class="table-detail-value">@table.Capacity @L["people"]</div>
                                </div>
                            </div>

                            <div class="table-detail-item">
                                <div class="table-detail-icon">
                                    <span class="material-symbols-outlined">link</span>
                                </div>
                                <div>
                                    <div class="table-detail-label">@L["Joinable"]</div>
                                    <div class="table-detail-value">
                                        @if (table.IsJoinable)
                                        {
                                            <span style="color: #2ECC71;">@L["Yes"]</span>
                                        }
                                        else
                                        {
                                            <span style="color: var(--text-muted);">@L["No"]</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="table-card-footer">
                            <a class="btn btn-sm btn-outline-primary table-action" href="@Url.Action("EditTable", "Tables", new { id = table.TableID })">
                                <span class="material-symbols-outlined" style="font-size: 1rem; vertical-align: middle;">edit</span>
                                <span class="action-text">Edit</span>
                            </a>
                            @if (table.IsAvailable)
                            {
                                <a class="btn btn-sm btn-outline-secondary table-action" href="@Url.Action("ToggleTableAvailability", "Tables", new { id = table.TableID })" title="@L["Make unavailable"]">
                                    <span class="material-symbols-outlined" style="font-size: 1rem; vertical-align: middle;">block</span>
                                    <span class="action-text">@L["Make unavailable"]</span>
                                </a>
                            }
                            else
                            {
                                <a class="btn btn-sm btn-outline-success table-action" href="@Url.Action("ToggleTableAvailability", "Tables", new { id = table.TableID })" title="@L["Make available"]">
                                    <span class="material-symbols-outlined" style="font-size: 1rem; vertical-align: middle;">check</span>
                                    <span class="action-text">@L["Make available"]</span>
                                </a>
                            }
                            <button class="btn btn-sm btn-outline-danger table-action"
                                    onclick="if(confirm('@L["Are you sure you want to delete Table {0}? This action cannot be undone.", table.TableNumber]')) window.location.href='@Url.Action("DeleteTable", "Tables", new { id = table.TableID })'">
                                <span class="material-symbols-outlined" style="font-size: 1rem; vertical-align: middle;">delete</span>
                                <span class="action-text">Delete</span>
                            </button>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="empty-state" style="margin-top: var(--spacing-3xl); padding: var(--spacing-3xl) var(--spacing-2xl);">
            <span class="material-symbols-outlined empty-state-icon">table_restaurant_off</span>
            <h3 style="margin-top: var(--spacing-xl); margin-bottom: var(--spacing-md);">@L["No Tables Yet"]</h3>
            <p style="margin-bottom: var(--spacing-xl);">@L["Add your first table to get started"]</p>
            <a class="btn btn-primary" href="@Url.Action("CreateTable", "Tables")">
                <span class="material-symbols-outlined" style="vertical-align: middle; margin-right: 0.5rem;">add_circle</span>
                @L["Add New Table"]
            </a>
        </div>
    }
</div>

<style>
/* Stats Row Container */
.stats-row-container {
    display: flex;
    gap: var(--spacing-lg);
    flex-wrap: wrap;
    justify-content: flex-start;
    margin-bottom: var(--spacing-2xl);
}

.stat-cube-inline {
    flex: 0 1 auto;
    min-width: 200px;
    max-width: 280px;
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-xl);
    background: var(--bg-card);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-xl);
    transition: all var(--transition-base);
    text-decoration: none;
}

[data-theme="dark"] .stat-cube-inline {
    background: rgba(58, 58, 58, 0.5);
    border-color: rgba(85, 85, 85, 0.4);
}

.stat-cube-inline:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
}

.stat-cube-clickable:hover {
    border-color: var(--color-primary-gold);
    cursor: pointer;
}

.stat-cube-icon {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.stat-cube-red .stat-cube-icon {
    background: linear-gradient(135deg, #E63946, #C41E3A);
}

.stat-cube-gold .stat-cube-icon {
    background: linear-gradient(135deg, #D4AF37, #B8941E);
}

.stat-cube-green .stat-cube-icon {
    background: linear-gradient(135deg, #2ECC71, #27AE60);
}

.stat-cube-blue .stat-cube-icon {
    background: linear-gradient(135deg, #3498DB, #2980B9);
}

.stat-cube-purple .stat-cube-icon {
    background: linear-gradient(135deg, #9B59B6, #8E44AD);
}

.stat-cube-icon .material-symbols-outlined {
    font-size: 1.75rem;
    color: white;
}

.stat-cube-content {
    flex: 1;
    min-width: 0;
}

.stat-cube-number {
    font-size: 2rem;
    font-weight: 800;
    color: var(--text-primary);
    margin: 0;
    line-height: 1;
}

.stat-cube-label {
    color: var(--text-secondary);
    margin: var(--spacing-xs) 0 0 0;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.stat-cube-label-large {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
    line-height: 1.2;
}

.stat-cube-sublabel {
    font-size: 0.75rem;
    color: var(--text-secondary);
    margin: 0;
    font-weight: 600;
}

.table-card {
    background: var(--bg-card);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-xl);
    overflow: hidden;
    transition: all var(--transition-base);
    height: 100%;
    display: flex;
    flex-direction: column;
}

[data-theme="dark"] .table-card {
    background: rgba(58, 58, 58, 0.5);
    border-color: rgba(85, 85, 85, 0.4);
}

.table-card:hover {
    /* No visual fill on hover � keep card appearance stable. */
}

.table-card .table-action .material-symbols-outlined {
    font-size: 0.9rem; /* slightly smaller icon */
}

/* tooltip-style popup for action text */
.table-card-footer .btn { position: relative; }
.table-card .action-text {
    position: absolute;
    bottom: calc(100% + 6px);
    left: 50%;
    transform: translateX(-50%);
    display: none;
    white-space: nowrap;
    background: var(--bg-card);
    color: var(--text-primary);
    border: 1px solid var(--border-primary);
    padding: 0.2rem 0.4rem;
    border-radius: 0.35rem;
    font-size: 0.75rem;
    font-weight: 600;
    line-height: 1;
    box-shadow: var(--shadow-sm);
    pointer-events: none;
    opacity: 0;
    transition: opacity 120ms ease-in-out;
}
.table-card .action-text::after {
    content: "";
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 0;
    border-left: 6px solid transparent;
    border-right: 6px solid transparent;
    border-top: 6px solid var(--bg-card);
}
.table-card .table-action:hover .action-text {
    display: block;
    opacity: 1;
}

/* make action buttons more compact */
.table-card-footer .btn {
    padding: 0.25rem 0.5rem;
    font-size: 0.85rem;
}

.table-card-header {
    padding: var(--spacing-lg);
    background: linear-gradient(135deg, rgba(230, 57, 70, 0.1), rgba(212, 175, 55, 0.1));
    border-bottom: 1px solid var(--border-primary);
    display: flex;
    justify-content: space-between;
    align-items: center;
}

[data-theme="dark"] .table-card-header {
    background: linear-gradient(135deg, rgba(230, 57, 70, 0.2), rgba(212, 175, 55, 0.2));
}

.table-number-badge {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-lg);
    background: var(--color-primary-gold);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
}

.table-number {
    font-size: 1.5rem;
    font-weight: 800;
    color: white;
}

.table-badges {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
    align-items: flex-end;
}

.table-status-badge {
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-full);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
}

.status-available {
    background: rgba(46, 204, 113, 0.2);
    color: #27AE60;
    border: 1px solid #2ECC71;
}

.status-unavailable {
    background: rgba(230, 57, 70, 0.2);
    color: #C0392B;
    border: 1px solid #E63946;
}

.table-card-body {
    padding: var(--spacing-lg);
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
}

.table-detail-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
}

.table-detail-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    background: rgba(212, 175, 55, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

[data-theme="dark"] .table-detail-icon {
    background: rgba(212, 175, 55, 0.15);
}

.table-detail-icon .material-symbols-outlined {
    font-size: 1.25rem;
    color: var(--color-primary-gold);
}

.table-detail-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    text-transform: uppercase;
    font-weight: 600;
    letter-spacing: 0.05em;
}

.table-detail-value {
    font-size: 1rem;
    color: var(--text-primary);
    font-weight: 600;
}

.table-card-footer {
    padding: var(--spacing-md) var(--spacing-lg);
    border-top: 1px solid var(--border-primary);
    display: flex;
    gap: var(--spacing-sm);
    justify-content: space-between;
}

.table-card-footer .btn {
    flex: 1;
}

.empty-state {
    text-align: center;
    padding: var(--spacing-3xl);
    background: var(--bg-card);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-xl);
}

[data-theme="dark"] .empty-state {
    background: rgba(58, 58, 58, 0.5);
    border-color: rgba(85, 85, 85, 0.4);
}

.empty-state-icon {
    font-size: 5rem;
    color: var(--text-muted);
    margin-bottom: var(--spacing-lg);
}

.empty-state h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-sm);
}

.empty-state p {
    color: var(--text-secondary);
    margin-bottom: var(--spacing-xl);
}
</style>

@section Scripts {
    <script>
        (function(){
            var toggle = document.getElementById('join-toggle');
            var panel = document.getElementById('join-panel');
            if(panel){
                // show panel when join button clicked
                var joinBtn = document.getElementById('join-toggle');
                if(joinBtn){
                    joinBtn.addEventListener('click', function(){
                        panel.style.display = panel.style.display === 'block' ? 'none' : 'block';
                    });
                }
            }

            // show action text only for the hovered button
            document.querySelectorAll('.table-card .table-action').forEach(function(btn){
                btn.addEventListener('mouseenter', function(){
                    var txt = this.querySelector('.action-text'); if(txt) txt.style.display = 'inline-block';
                });
                btn.addEventListener('mouseleave', function(){
                    var txt = this.querySelector('.action-text'); if(txt) txt.style.display = 'none';
                });
            });
        })();
    </script>
}
